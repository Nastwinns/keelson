//! `haw-compliance` — a haw plugin binary (skeleton). Real behavior filled in by the
//! plugin build wave. Dispatched by haw as `haw compliance` -> `haw-compliance`.

fn main() {
    println!("haw-compliance: not implemented yet (skeleton)");
}

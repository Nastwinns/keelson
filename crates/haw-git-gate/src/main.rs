//! `haw-git-gate` — a haw plugin binary (skeleton). Real behavior filled in by the
//! plugin build wave. Dispatched by haw as `haw git-gate` -> `haw-git-gate`.

fn main() {
    println!("haw-git-gate: not implemented yet (skeleton)");
}

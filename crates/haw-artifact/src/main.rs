//! `haw-artifact` — a haw plugin binary (skeleton). Real behavior filled in by the
//! plugin build wave. Dispatched by haw as `haw artifact` -> `haw-artifact`.

fn main() {
    println!("haw-artifact: not implemented yet (skeleton)");
}
